{
  "name": "prettier-plugin-wp-block-html",
  "version": "0.1.0",
  "description": "Prettier plugin for HTML in WordPress Block Theme.",
  "keywords": [],
  "author": "Nao Komura",
  "license": "MIT",
  "packageManager": "npm@9.4.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild ./src/plugin.ts --bundle --platform=node --packages=external --outfile=./dist/index.js",
    "format": "prettier --write ./test/test.html --plugin ./dist",
    "test": "node ./test/prettier.js"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "@types/prettier": "^2.7.2",
    "esbuild": "^0.17.5",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "prettier": "^2.8.3"
  }
}
